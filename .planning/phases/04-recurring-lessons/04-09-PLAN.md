---
phase: 04-recurring-lessons
plan: "09"
type: execute
wave: 4
depends_on:
  - "04-06"
  - "04-07"
  - "04-08"
files_modified: []
autonomous: false

requirements:
  - CRED-01
  - CRED-02
  - CRED-03
  - CRED-04
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - LES-01
  - LES-02
  - LES-03
  - RESC-01
  - RESC-02
  - RESC-03
  - TDASH-01
  - TDASH-02
  - TDASH-03
  - TDASH-04
  - SDASH-01
  - SDASH-02
  - SDASH-03
  - SDASH-04

must_haves:
  truths:
    - "Full Phase 4 recurring lesson system is live and end-to-end tested by a human"
    - "Credit wallet shows correct balance and transaction history"
    - "Recurring schedules can be created, paused, resumed, and cancelled"
    - "Cron generates lessons correctly (idempotent)"
    - "Reschedule workflow: student requests, teacher approves/rejects, no credit loss"
    - "Teacher and student lesson dashboards render and function correctly"
  artifacts: []
  key_links: []
---

<objective>
Human UAT checkpoint — verify the complete Phase 4 recurring lesson system end-to-end in the browser.

Purpose: Automated tests cover logic; this checkpoint covers the integrated user experience: navigation works, data flows correctly, forms submit and produce visible results.
Output: User confirmation that all Phase 4 requirements are met.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-recurring-lessons/04-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">\n  <name>Phase 4 UAT: Full system verification</name>\n  <action>Human verification of complete Phase 4 recurring lessons system. Follow the how-to-verify checklist below.</action>
  <what-built>
Complete Phase 4: Recurring Lessons system including:
- DB migration: credit_wallets, credit_transactions, recurring_schedules, lessons tables (+ 3 RPCs)
- Student credit wallet (replaces raw credits balance) with full transaction history
- Recurring schedule creation, pause, resume, cancel
- Lesson auto-generation via Vercel Cron (daily 00:00, 6-week look-ahead, idempotent)
- Reschedule workflow: student requests → teacher approves (atomic) or rejects
- Teacher lessons dashboard: active students + credits, schedules, upcoming lessons, reschedule queue
- Student lessons dashboard: wallet balance + history, schedules, upcoming lessons + reschedule CTA
  </what-built>
  <how-to-verify>
Run `npm run dev` (delete .next first if encountering 404s on new routes — see Turbopack cache memory).

**UAT Checklist — complete each step and confirm:**

--- Credit Wallet ---
[ ] 1. Log in as a student → navigate to /student/lessons
[ ] 2. Credit wallet balance is displayed prominently (StatCard showing numeric balance)
[ ] 3. Transaction history section appears — if the student previously bought credits (Phase 3), at least one "Opening balance" transaction appears

--- Recurring Schedules ---
[ ] 4. On the student lessons page: verify a "Recurring Schedules" section exists (may be empty for new student)
[ ] 5. To test schedule creation — you will need a UI entry point. Note: Plan 04-08 has the student lessons page but NOT a "create schedule" form (that is a teacher-profile or separate flow). Verify that the student lessons page at minimum shows the schedules section (even if empty). If you want to manually insert a test schedule: use Supabase Studio → recurring_schedules → INSERT row with status='active', weekday=0 (Monday), start_time='16:00', end_time='16:50', valid student_id, teacher_id.
[ ] 6. After inserting a test schedule: reload /student/lessons — schedule appears with weekday name ("Monday"), time, and status badge "Active"
[ ] 7. Click Pause → page reloads, schedule shows "Paused" status
[ ] 8. Click Resume → page reloads, schedule shows "Active" status
[ ] 9. Click Cancel → page reloads, schedule shows "Cancelled" status (no buttons)

--- Lesson Generation (Cron) ---
[ ] 10. Ensure test schedule is in "Active" status (INSERT or update back to active)
[ ] 11. Trigger cron manually: `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/generate-lessons` (replace $CRON_SECRET with value from .env.local)
[ ] 12. Response body shows `{"ok":true,"generated":N}` where N > 0
[ ] 13. In Supabase Studio → lessons table: rows appear for the test schedule, all with status='confirmed', correct weekday dates
[ ] 14. Run the curl command again → response shows `{"ok":true,"generated":0}` (idempotent — no duplicates)

--- Teacher Dashboard ---
[ ] 15. Log in as a teacher → navigate to /teacher/lessons
[ ] 16. "Active Students" section shows the student who has an active schedule with you (and their credit balance)
[ ] 17. "Recurring Schedules" section shows the test schedule with student name
[ ] 18. "Upcoming Lessons" section shows the generated lessons sorted by date
[ ] 19. Click "Mark Complete" on a lesson → lesson disappears from upcoming, reappears in student's transaction history as "completion_deduction"

--- Student Upcoming Lessons ---
[ ] 20. Log in as student → /student/lessons → Upcoming Lessons section shows generated lessons
[ ] 21. Click Reschedule on a confirmed lesson → form with datetime inputs appears
[ ] 22. Enter a future date/time in both fields → Submit → lesson status changes to "Reschedule Pending" badge

--- Reschedule Approval ---
[ ] 23. Log in as teacher → /teacher/lessons → Reschedule Requests section shows the pending request
[ ] 24. Click "Approve" → original lesson disappears, new lesson appears at proposed time in Upcoming Lessons
[ ] 25. Student's /student/lessons → confirms new lesson time appears (no credit change — balance unchanged)
[ ] 26. Repeat steps 21-22, then as teacher click "Reject" → lesson reverts to "Confirmed" status in student view

--- Navigation ---
[ ] 27. Teacher nav has a "Lessons" link → clicks correctly to /teacher/lessons
[ ] 28. Student nav has a "Lessons" link → clicks correctly to /student/lessons
[ ] 29. Switch between locales (EN/DE) — lesson pages load without errors

--- Credit Safety ---
[ ] 30. After all tests: verify student's credit balance only decreased when a lesson was marked Complete (step 19) — not on cancel, reschedule request, approve, or reject
  </how-to-verify>
  <resume-signal>
Type "approved" if all checklist items pass, or describe which items failed (e.g., "step 12 failed — cron returns 0 generated") so Claude can investigate.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation. All 30 checklist items checked.
</verification>

<success_criteria>
User types "approved" after verifying all 30 UAT checklist items. Phase 4 is complete.
</success_criteria>

<output>
After the checkpoint is approved, create `.planning/phases/04-recurring-lessons/04-09-SUMMARY.md`
</output>
