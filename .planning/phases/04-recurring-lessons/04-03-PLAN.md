---
phase: 04-recurring-lessons
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - lib/utils/schedule.ts
  - lib/queries/wallet.ts
  - lib/queries/lessons.ts
autonomous: true
requirements:
  - CRED-01
  - CRED-04
  - LES-01
  - LES-02
  - TDASH-01
  - TDASH-03
  - SDASH-01
  - SDASH-02
  - SDASH-04

must_haves:
  truths:
    - "computeOccurrences utility correctly converts JS weekday to ISO weekday using (jsDay+6)%7"
    - "computeOccurrences returns all matching weekday occurrences in the given date window, none outside it"
    - "getWalletBalance returns available_balance from credit_wallets for a given student_id"
    - "getCreditTransactions returns rows from credit_transactions ordered by created_at DESC"
    - "getTeacherActiveStudents joins recurring_schedules to credit_wallets to return student names and balances"
    - "getStudentUpcomingLessons filters status IN ('confirmed','reschedule_requested') AND start_time >= now()"
    - "getTeacherUpcomingLessons filters same conditions for teacher_id"
    - "schedule.util tests go GREEN after this plan"
    - "wallet.test.ts goes GREEN after this plan"
  artifacts:
    - path: "lib/utils/schedule.ts"
      provides: "computeOccurrences utility — ISO weekday math"
      exports: ["computeOccurrences"]
    - path: "lib/queries/wallet.ts"
      provides: "getWalletBalance, getCreditTransactions"
      exports: ["getWalletBalance", "getCreditTransactions"]
    - path: "lib/queries/lessons.ts"
      provides: "getStudentUpcomingLessons, getTeacherUpcomingLessons, getTeacherActiveStudents"
      exports: ["getStudentUpcomingLessons", "getTeacherUpcomingLessons", "getTeacherActiveStudents"]
  key_links:
    - from: "lib/utils/schedule.ts"
      to: "app/api/cron/generate-lessons/route.ts"
      via: "import computeOccurrences"
      pattern: "import.*computeOccurrences.*from.*lib/utils/schedule"
    - from: "lib/queries/wallet.ts"
      to: "app/[locale]/student/lessons/page.tsx"
      via: "getWalletBalance called in Server Component"
      pattern: "getWalletBalance"
---

<objective>
Implement the `computeOccurrences` utility and all data query functions that the cron route and dashboard pages will consume.

Purpose: Shared utilities and queries must exist before the cron route and page plans run. Turning RED test stubs GREEN validates correctness before the full system is wired.
Output: `lib/utils/schedule.ts`, `lib/queries/wallet.ts`, `lib/queries/lessons.ts`
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-recurring-lessons/04-RESEARCH.md
@.planning/phases/04-recurring-lessons/04-CONTEXT.md
@.planning/phases/04-recurring-lessons/04-01-SUMMARY.md
@lib/queries/bookings.ts
</context>

<interfaces>
<!-- Key types from prior plans. Executor uses these directly. -->

From lib/types/database.types.ts (generated in Plan 04-01):
The following tables now exist and are typed:
- `credit_wallets`: student_id, available_balance, updated_at
- `credit_transactions`: id, student_id, lesson_id (nullable), amount, type, description, created_at
- `recurring_schedules`: id, student_id, teacher_id, weekday, start_time, end_time, status, created_at, updated_at
- `lessons`: id, student_id, teacher_id, schedule_id (nullable), start_time, end_time, meet_link, status, reschedule_proposed_start, reschedule_proposed_end, reschedule_requested_by, created_at, updated_at

From lib/queries/bookings.ts (existing pattern to follow):
```typescript
import { createClient } from "@/lib/supabase/server";
// All query functions are async, take userId or profileId as first arg
// Supabase join results typed as arrays — use Array.isArray() + [0] indexing
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeOccurrences utility (lib/utils/schedule.ts)</name>
  <files>lib/utils/schedule.ts</files>
  <behavior>
    - Input: schedule object with {id, student_id, teacher_id, weekday: number (0=Mon), start_time: "HH:MM", end_time: "HH:MM"}, from: Date, to: Date
    - ISO conversion: `const isoDay = (date.getDay() + 6) % 7` — a Monday (getDay()=1) maps to isoDay=0 ✓
    - Returns only dates where isoDay === schedule.weekday
    - start_time for each occurrence is `from` date or later (exclusive — `startTs > from` per RESEARCH.md pattern)
    - end_time is same calendar day as start_time, using schedule.end_time hours/minutes
    - Result item shape: `{ schedule_id, student_id, teacher_id, start_time: ISO string, end_time: ISO string, status: "confirmed" }`
    - Empty array if no matching weekday falls in the window
  </behavior>
  <action>
Create `lib/utils/schedule.ts` exactly as specified in RESEARCH.md Code Examples section ("ISO weekday utility").

Key points:
- Export `computeOccurrences` as a named export
- Cursor starts at `from`, increments by 1 day
- ISO weekday formula: `const isoDay = (cursor.getDay() + 6) % 7`
- Only push if `startTs > from` (skip occurrences at the exact boundary)
- Use `.toISOString()` for start_time and end_time in result objects
- Do NOT export a default — named export only (consistent with project pattern)

Also export a TypeScript type for the input schedule and output occurrence:
```typescript
export interface ScheduleInput {
  id: string;
  student_id: string;
  teacher_id: string;
  weekday: number;
  start_time: string;
  end_time: string;
}

export interface LessonOccurrence {
  schedule_id: string;
  student_id: string;
  teacher_id: string;
  start_time: string;
  end_time: string;
  status: "confirmed";
}
```
  </action>
  <verify>
    <automated>cd "C:/Academigo/academigo/app_academigo" && npm test -- --testPathPattern="schedule.util" 2>&1 | tail -10</automated>
  </verify>
  <done>schedule.util.test.ts passes (GREEN). computeOccurrences correctly handles Monday=0, produces correct ISO timestamps, returns empty array for empty windows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wallet queries and lesson queries</name>
  <files>lib/queries/wallet.ts, lib/queries/lessons.ts</files>
  <behavior>
    **wallet.ts:**
    - getWalletBalance(studentId: string): Promise&lt;number&gt; — SELECT available_balance FROM credit_wallets WHERE student_id = studentId; returns 0 if not found
    - getCreditTransactions(studentId: string): Promise&lt;CreditTransaction[]&gt; — SELECT * FROM credit_transactions WHERE student_id = studentId ORDER BY created_at DESC
    - No TTL/expiry filter in either query (CRED-04)

    **lessons.ts:**
    - getStudentUpcomingLessons(studentId: string): fetches lessons WHERE student_id = studentId AND status IN ('confirmed','reschedule_requested') AND start_time >= now() ORDER BY start_time ASC; joins teachers.profiles for teacher name
    - getTeacherUpcomingLessons(teacherId: string): same filters but WHERE teacher_id = teacherId; joins students.profiles for student name
    - getTeacherActiveStudents(teacherId: string): fetches distinct students from active recurring_schedules for teacher; includes credit balance from credit_wallets; returns array of { studentId, studentName, availableCredits }
    - getTeacherSchedules(teacherId: string): SELECT all recurring_schedules WHERE teacher_id = teacherId; joins students.profiles for student name; ORDER BY created_at DESC (used by plan 04-07)
    - getStudentSchedules(studentId: string): SELECT all recurring_schedules WHERE student_id = studentId; joins teachers.profiles for teacher name; ORDER BY created_at DESC (used by plan 04-08)
    - getRescheduleRequests(teacherId: string): SELECT lessons WHERE teacher_id = teacherId AND status = 'reschedule_requested' AND start_time >= now() ORDER BY start_time ASC (used by plan 04-07)
  </behavior>
  <action>
Create `lib/queries/wallet.ts`:
- `"use server"` directive at top (follows project pattern)
- Import `createClient` from `@/lib/supabase/server`
- `getWalletBalance`: query credit_wallets, return `data?.available_balance ?? 0`
- `getCreditTransactions`: query credit_transactions, return `data ?? []`
- Export types: `CreditTransaction` matching the DB row shape

Create `lib/queries/lessons.ts`:
- `"use server"` directive at top
- Import `createClient` from `@/lib/supabase/server`
- For join fields (teachers/students/profiles), follow the `Array.isArray` pattern from `lib/queries/bookings.ts` — Supabase infers FK joins as arrays even when it's a 1:1 relationship (project decision in STATE.md)
- `getTeacherActiveStudents`: Use a join from recurring_schedules to students (for name via profiles) and separately call `student_available_credits(student_id)` RPC, or join to credit_wallets directly via `students!inner(profiles!inner(full_name), credit_wallets!inner(available_balance))`
- Export all 6 functions as named exports
  </action>
  <verify>
    <automated>cd "C:/Academigo/academigo/app_academigo" && npm test -- --testPathPattern="wallet" 2>&1 | tail -10</automated>
  </verify>
  <done>wallet.test.ts passes (GREEN). lib/queries/wallet.ts and lib/queries/lessons.ts exist with all exported functions. `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern="schedule.util|wallet"` — all pass GREEN
2. `npx tsc --noEmit` — no type errors in lib/utils/schedule.ts, lib/queries/wallet.ts, lib/queries/lessons.ts
3. `grep "computeOccurrences\|getWalletBalance\|getCreditTransactions" lib/utils/schedule.ts lib/queries/wallet.ts` — all exports present
</verification>

<success_criteria>
schedule.util.test.ts and wallet.test.ts both GREEN. All query functions in lessons.ts exist and are typed. TypeScript compilation passes. computeOccurrences handles ISO vs JS weekday correctly (verified by test).
</success_criteria>

<output>
After completion, create `.planning/phases/04-recurring-lessons/04-03-SUMMARY.md`
</output>
