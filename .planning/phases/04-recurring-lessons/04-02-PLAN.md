---
phase: 04-recurring-lessons
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/utils/schedule.util.test.ts
  - __tests__/lib/actions/schedules.test.ts
  - __tests__/lib/actions/reschedule.test.ts
  - __tests__/lib/queries/wallet.test.ts
  - __tests__/api/cron/generate-lessons.test.ts
autonomous: true
requirements:
  - CRED-01
  - CRED-03
  - CRED-04
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - LES-01
  - LES-02
  - RESC-01
  - RESC-02
  - RESC-03

must_haves:
  truths:
    - "5 test files exist with failing RED stubs covering all logic-testable Phase 4 requirements"
    - "Tests fail with 'Cannot find module' or explicit RED assertion — not due to test file syntax errors"
    - "makeChainable factory pattern used consistently — no top-level const mock hoisting issues"
    - "Dynamic import inside test body used for action/query modules (Phase 3 pattern for RED phase)"
  artifacts:
    - path: "__tests__/lib/utils/schedule.util.test.ts"
      provides: "LES-01, LES-02 — computeOccurrences unit tests"
    - path: "__tests__/lib/actions/schedules.test.ts"
      provides: "SCHED-01, SCHED-02, CRED-03 — createSchedule, updateScheduleStatus"
    - path: "__tests__/lib/actions/reschedule.test.ts"
      provides: "RESC-01, RESC-02, RESC-03 — requestReschedule, approveReschedule, rejectReschedule"
    - path: "__tests__/lib/queries/wallet.test.ts"
      provides: "CRED-01, CRED-04 — getWalletBalance"
    - path: "__tests__/api/cron/generate-lessons.test.ts"
      provides: "SCHED-03, LES-02 — cron auth guard, skips paused schedules"
  key_links:
    - from: "__tests__/lib/utils/schedule.util.test.ts"
      to: "lib/utils/schedule.ts"
      via: "dynamic import"
      pattern: "import.*lib/utils/schedule"
    - from: "__tests__/lib/actions/schedules.test.ts"
      to: "lib/actions/schedules.ts"
      via: "dynamic import inside test body"
      pattern: "import.*lib/actions/schedules"
---

<objective>
Write all Wave 0 RED test stubs for Phase 4. Every file must fail with "Cannot find module" until the corresponding implementation plan runs.

Purpose: Test-first cycle ensures behaviors are locked before implementation. Failures prove tests are active, not silent.
Output: 5 test files, all RED.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-recurring-lessons/04-RESEARCH.md
@.planning/phases/04-recurring-lessons/04-CONTEXT.md
@__tests__/api/cron/reminders.test.ts
@__tests__/lib/actions/bookings.test.ts
</context>

<feature>
  <name>Phase 4 RED test stubs</name>
  <files>
    __tests__/lib/utils/schedule.util.test.ts,
    __tests__/lib/actions/schedules.test.ts,
    __tests__/lib/actions/reschedule.test.ts,
    __tests__/lib/queries/wallet.test.ts,
    __tests__/api/cron/generate-lessons.test.ts
  </files>
  <behavior>
All tests must fail in RED phase. Behavioral assertions are:

**schedule.util.test.ts (LES-01, LES-02):**
- computeOccurrences with weekday=0 (Monday) returns only dates that fall on Monday
- ISO weekday conversion: `(jsDay + 6) % 7` — a date with getDay()=1 (Tuesday) maps to isoDay=0 (Monday) — WRONG, this should be isoDay=1 (Monday). The formula for Monday is getDay()=1 → (1+6)%7=0 ✓
- Given a 6-week window, returns all matching weekday occurrences (no more, no less)
- Returns empty array if no matching weekdays in window
- Results have schedule_id, student_id, teacher_id, start_time (ISO), end_time (ISO), status='confirmed'
- Does not return occurrences in the past (start_time <= from)

**schedules.test.ts (SCHED-01, SCHED-02, CRED-03):**
- createSchedule calls supabase insert on recurring_schedules with correct shape
- createSchedule requires role='student' (calls requireRole)
- createSchedule returns error if teacherId missing
- updateScheduleStatus with 'paused' updates status field
- updateScheduleStatus with 'cancelled' updates status field
- updateScheduleStatus with 'active' updates status field (resume)
- Neither createSchedule nor updateScheduleStatus touches credit_wallets or calls complete_lesson RPC (CRED-03)

**reschedule.test.ts (RESC-01, RESC-02, RESC-03):**
- requestReschedule updates lesson status to 'reschedule_requested' and sets proposed start/end
- requestReschedule accepts lesson already in 'reschedule_requested' status (re-request case from RESEARCH.md Pitfall 3)
- approveReschedule calls supabase.rpc('approve_reschedule') with lesson id
- approveReschedule does NOT touch credit_wallets (RESC-03 — no credit loss)
- rejectReschedule calls supabase.rpc('reject_reschedule') with lesson id

**wallet.test.ts (CRED-01, CRED-04):**
- getWalletBalance calls supabase.from('credit_wallets').select().eq() for correct student_id
- getWalletBalance returns available_balance number
- No TTL/expiry field present in the select query (CRED-04)

**generate-lessons.test.ts (SCHED-03, LES-02):**
- GET without Authorization header returns 401
- GET with wrong secret returns 401
- GET with correct CRON_SECRET returns 200 and { ok: true }
- Cron only queries schedules with status='active' (SCHED-03 — paused/cancelled skipped)
  </behavior>
  <implementation>
All test files use the following patterns (MANDATORY — Phase 3 project decisions):
1. `const mocks = { supabase: {...}, ... }` object at top — NOT top-level `const mockFn = jest.fn()` (avoids Jest hoisting TDZ)
2. `makeChainable(resolves)` factory for Supabase fluent API (consistent with existing test files)
3. `dynamic import inside test body` for action/query modules: `const { createSchedule } = await import("@/lib/actions/schedules")` — ensures tests remain RED until modules exist
4. Mock `@/lib/supabase/service` in cron tests (not `@/lib/supabase/server`) since cron uses `createServiceClient()`
5. `jest.mock(...)` calls at top level, before `describe` blocks

See `__tests__/api/cron/reminders.test.ts` and `__tests__/lib/actions/bookings.test.ts` for the exact pattern to follow.
  </implementation>
</feature>

<verification>
  <automated>cd "C:/Academigo/academigo/app_academigo" && npm test -- --testPathPattern="schedule.util|schedules|reschedule|wallet|generate-lessons" --passWithNoTests 2>&1 | tail -20</automated>
</verification>

<success_criteria>
All 5 test files created. Running the test suite shows FAIL status for each file with "Cannot find module" errors (or explicit RED assertion failures). Zero syntax errors in test files (Jest can parse them). `npm test -- --passWithNoTests` exits cleanly (no jest config errors).
</success_criteria>

<output>
After completion, create `.planning/phases/04-recurring-lessons/04-02-SUMMARY.md`
</output>
