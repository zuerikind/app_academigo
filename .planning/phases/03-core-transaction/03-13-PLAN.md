---
phase: 03-core-transaction
plan: "13"
type: execute
wave: 5
depends_on:
  - "03-07"
  - "03-08"
  - "03-09"
  - "03-10"
  - "03-11"
  - "03-12"
files_modified: []
autonomous: false
requirements:
  - AVAIL-01
  - AVAIL-02
  - AVAIL-03
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - BOOK-04
  - BOOK-05
  - BOOK-06
  - BOOK-07
  - BOOK-08
  - BOOK-09
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05
  - REV-01
  - REV-02
  - REV-03
  - REV-04
  - EARN-01
  - EARN-02
  - EARN-03
  - EARN-04
  - EARN-05

must_haves:
  truths:
    - "Full test suite passes: npm test exits 0"
    - "Teacher can set availability and student sees correct slots on teacher profile"
    - "End-to-end booking cycle works: request → confirm (with meet link) → join lesson → mark complete → review"
    - "Stripe Checkout redirects to Stripe and credits are granted after webhook"
    - "Teacher reminder emails are dispatched by cron with no duplicates"
    - "Teacher earnings page shows correct balance and payout request works"
    - "Admin Missing Meet Links page shows upcoming lessons without meet links"
  artifacts:
    - path: "app/[locale]/teacher/availability/page.tsx"
      provides: "Working availability management page"
    - path: "app/[locale]/teacher/bookings/page.tsx"
      provides: "Working teacher booking queue with Meet Link Status"
    - path: "app/[locale]/student/teachers/[id]/page.tsx"
      provides: "Working teacher profile with calendar + booking form"
    - path: "app/[locale]/student/bookings/page.tsx"
      provides: "Working student bookings page with Join/Waiting UX and review forms"
    - path: "app/[locale]/student/packages/page.tsx"
      provides: "Working packages page with Stripe buy buttons"
    - path: "app/[locale]/teacher/earnings/page.tsx"
      provides: "Working earnings page with payout request"
    - path: "app/[locale]/admin/missing-links/page.tsx"
      provides: "Working admin missing links view"
  key_links:
    - from: "Stripe Checkout"
      to: "app/api/webhooks/stripe/route.ts"
      via: "Webhook fires on checkout.session.completed; credits granted via RPC"
      pattern: "grant_credits|grant_subscription_credits"
    - from: "confirmBooking action"
      to: "student email inbox"
      via: "sendBookingConfirmation via Resend"
      pattern: "sendBookingConfirmation"
---

<objective>
Human verification checkpoint for all Phase 3 deliverables. Confirms the full booking cycle, payment flow, email notifications, cron behavior, and admin monitoring views work correctly in the browser.

Purpose: Automated tests verify function contracts; this checkpoint verifies rendered UI and end-to-end flows that only a human can confirm.
Output: Phase 3 verified and marked complete.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite gate</name>
  <files></files>
  <action>
Run the full test suite and confirm all tests pass before proceeding to human verification.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
`npm test` exits with code 0. All test files GREEN. Zero failures.

If any tests are RED: stop, diagnose, fix in the appropriate plan's files before proceeding to human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — full Phase 3 end-to-end verification</name>
  <files></files>
  <action>
Pause for human verification of all Phase 3 flows in the browser.

What was built: Complete Phase 3 — availability management, full booking cycle with Google Meet links, Stripe credit purchases, post-session reviews, teacher earnings and payout requests, email notifications, Vercel Cron reminders, and admin missing meet links view.

How to verify:

Start the dev server: `npm run dev` (delete `.next/` first if new routes return 404).

**A. Availability Management (AVAIL-01/02/03)**
1. Sign in as a teacher. Navigate to `/teacher/availability`.
2. Add a recurring range: Monday, 14:00–18:00. Verify it appears in the list.
3. Remove the range. Verify it disappears.
4. Sign in as a student. Visit that teacher's profile page (`/student/teachers/[id]`).
5. Confirm the "Book a Session" section shows a monthly calendar. Navigate to next month.
6. Click on a Monday. Confirm slots appear (e.g. 14:00, 14:15, 14:30...).

**B. Booking Cycle (BOOK-01 through BOOK-09)**
1. As student: click a slot, fill topic note, submit booking request. Confirm "Booking requested" success.
2. As teacher: navigate to `/teacher/bookings`. See the pending request.
3. In the Confirm form: verify default_meet_link is pre-populated (or warning shown if not set).
4. Confirm the booking with a Meet link. Verify status changes to "confirmed".
5. As student: on `/student/bookings`, see the booking as "confirmed" with **Join Lesson** button.
6. As teacher: click **Mark Complete** on a past booking. Verify status changes to "completed".
7. As teacher: add/update meet link inline on a confirmed booking. Verify Meet Link Status changes.
8. As student: cancel a pending booking. Verify booking moves to cancelled.

**C. Stripe Payments (PAY-01 through PAY-05)**
1. As student: visit `/student/packages`. Verify credit balance is displayed.
2. Click "Buy" on Essentials Single. Confirm redirect to Stripe Checkout.
3. Use Stripe test card `4242 4242 4242 4242`, any future expiry, any CVC.
4. Confirm redirect to `/student/packages?success=true` with success banner.
5. Verify credit balance increased by 1.

**D. Reviews (REV-01 through REV-04)**
1. As student: on `/student/bookings`, click "Leave a review" on a completed booking.
2. Verify form expands inline (no modal, no navigation).
3. Select 4 stars, add comment, submit.
4. Verify "Review submitted. Thank you!" appears and Leave a review button disappears.
5. Visit teacher profile. Verify review appears with avg rating and count.

**E. Earnings and Payout (EARN-02/03)**
1. As teacher: navigate to `/teacher/earnings` via sidebar nav link.
2. Verify pending balance displayed correctly.
3. Click "Request payout". Confirm success.
4. As admin: verify payout in `/admin/payouts`. Mark as processed.

**F. Admin Missing Meet Links**
1. Create a confirmed booking without a meet link.
2. As admin: navigate to `/admin/missing-links`.
3. Verify booking appears in table with teacher name, student name, hours until lesson.

**G. Cron Reminder (manual trigger)**
1. `curl -H "Authorization: Bearer <CRON_SECRET>" http://localhost:3000/api/cron/reminders`
2. Verify JSON `{ ok: true, sent24h: ..., sent1h: ... }`.
3. Trigger again — verify no duplicate sends (idempotent).

Resume signal: Type "approved" when all flows verified, or describe specific issues found.
  </action>
  <verify>
    <automated>MISSING — manual verification required for browser flows</automated>
  </verify>
  <done>
Human has typed "approved" confirming all Phase 3 requirements are met in the browser:
- All navigation links work (no 404s on Phase 3 pages)
- Full booking cycle from slot selection through review verified
- Stripe Checkout redirect and credit grant verified with test card
- Meet Link Status indicators work correctly
- Join Lesson / Waiting for teacher UX works on student bookings page
- Inline review form expands without page navigation
- Teacher earnings page and payout request work
- Admin missing-links page populated correctly
- Cron endpoint secured and idempotent
  </done>
</task>

</tasks>

<verification>
Phase 3 complete when:
- [ ] `npm test` exits 0 (all tests GREEN)
- [ ] Teacher availability management works in browser
- [ ] Full booking cycle verified: request → confirm → join → complete → review
- [ ] Stripe Checkout redirect and credit grant verified with test card
- [ ] Meet Link Status indicators work on teacher bookings page
- [ ] Join Lesson / Waiting for teacher UX works on student bookings page
- [ ] Inline review form expands and submits without page navigation
- [ ] Teacher earnings page shows balance and payout submission works
- [ ] Admin missing-links page populated correctly
- [ ] Human types "approved"
</verification>

<success_criteria>
All 25 Phase 3 requirements verified:
- AVAIL-01/02/03: Availability management system working
- BOOK-01 through BOOK-09: Full booking state machine working end-to-end
- PAY-01 through PAY-05: Stripe Checkout and webhook credit grant working
- REV-01 through REV-04: Post-session reviews working; teacher profile shows aggregate
- EARN-01 through EARN-05: Earnings recording, teacher view, and payout submission working
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction/03-13-SUMMARY.md`
</output>
