---
phase: 03-core-transaction
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/utils/slots.test.ts
  - __tests__/lib/actions/availability.test.ts
  - __tests__/lib/actions/bookings.test.ts
  - __tests__/lib/actions/payments.test.ts
  - __tests__/api/webhooks/stripe.test.ts
  - __tests__/lib/actions/reviews.test.ts
  - __tests__/lib/actions/earnings.test.ts
autonomous: true
requirements: [AVAIL-01, AVAIL-02, AVAIL-03, BOOK-01, BOOK-02, BOOK-04, BOOK-07, BOOK-08, BOOK-09, PAY-02, PAY-03, REV-01, EARN-03]

must_haves:
  truths:
    - "Seven RED test stub files exist and fail with 'Cannot find module' (not syntax errors)"
    - "All test files use the mocks object pattern consistent with admin.test.ts and auth.test.ts"
    - "npm test -- --passWithNoTests exits with failed suite (RED) not syntax failure"
  artifacts:
    - path: "__tests__/lib/utils/slots.test.ts"
      provides: "AVAIL-03 slot generation stubs"
    - path: "__tests__/lib/actions/availability.test.ts"
      provides: "AVAIL-01/02 stubs"
    - path: "__tests__/lib/actions/bookings.test.ts"
      provides: "BOOK-01/02/04/07/08/09 stubs"
    - path: "__tests__/lib/actions/payments.test.ts"
      provides: "PAY-02 stubs"
    - path: "__tests__/api/webhooks/stripe.test.ts"
      provides: "PAY-03 idempotency + signature rejection stubs"
    - path: "__tests__/lib/actions/reviews.test.ts"
      provides: "REV-01 stubs"
    - path: "__tests__/lib/actions/earnings.test.ts"
      provides: "EARN-03 stubs"
  key_links:
    - from: "__tests__/lib/actions/bookings.test.ts"
      to: "lib/actions/bookings.ts"
      via: "import { requestBooking, confirmBooking, ... }"
      pattern: "Cannot find module"
---

<objective>
Create Wave 0 RED test stubs for all Phase 3 testable behaviors.

Purpose: Nyquist compliance — every automated verify command in downstream plans references a test file that exists. Tests fail with "Cannot find module" (correct RED state) until the implementation plans complete them.
Output: Seven test stub files, all failing correctly in RED.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-transaction/03-CONTEXT.md
@.planning/phases/03-core-transaction/03-RESEARCH.md
@.planning/phases/03-core-transaction/03-VALIDATION.md

Read __tests__/lib/actions/admin.test.ts and __tests__/lib/actions/auth.test.ts first — these establish the exact mocks object pattern and makeChainable factory pattern to follow. ALL test stubs MUST use the same pattern to avoid Jest hoisting temporal dead zone issues.
</context>

<feature>
  <name>Phase 3 Wave 0 RED test stubs</name>
  <files>
    __tests__/lib/utils/slots.test.ts,
    __tests__/lib/actions/availability.test.ts,
    __tests__/lib/actions/bookings.test.ts,
    __tests__/lib/actions/payments.test.ts,
    __tests__/api/webhooks/stripe.test.ts,
    __tests__/lib/actions/reviews.test.ts,
    __tests__/lib/actions/earnings.test.ts
  </files>
  <behavior>
    RED Phase — all files fail with "Cannot find module" on import. Not syntax errors. This is the correct state.

    **__tests__/lib/utils/slots.test.ts** (AVAIL-03):
    - generateSlots returns [] when targetDate is in blockedDates
    - generateSlots returns 15-min increment slots for a given range
    - generateSlots filters out slots that overlap with existing bookings (status pending or confirmed)
    - generateSlots slot end time is start + 50 minutes (lessonDurationMinutes)

    **__tests__/lib/actions/availability.test.ts** (AVAIL-01/02):
    - setAvailabilityRange saves range to teacher_availability_ranges
    - setAvailabilityRange returns error if not authenticated as teacher
    - removeAvailabilityRange deletes range by id
    - setBlocker saves blocked_date to teacher_availability_blockers
    - removeBlocker deletes blocker by id

    **__tests__/lib/actions/bookings.test.ts** (BOOK-01/02/04/07/08/09):
    - requestBooking calls create_booking RPC with correct params
    - requestBooking returns { error: "Not enough credits..." } on insufficient_credits RPC error
    - confirmBooking sets status='confirmed' and meeting_link on the booking
    - confirmBooking returns error if meetingLink is missing
    - declineBooking calls cancel_booking RPC
    - markComplete calls complete_booking RPC
    - cancelBooking calls cancel_booking RPC

    **__tests__/lib/actions/payments.test.ts** (PAY-02):
    - createCheckoutSession redirects to Stripe session URL
    - createCheckoutSession returns error if package not found
    - createCheckoutSession returns error if student record not found

    **__tests__/api/webhooks/stripe.test.ts** (PAY-03):
    - POST returns 400 if stripe-signature header is missing
    - POST returns 400 if webhook signature verification fails
    - POST processes checkout.session.completed and calls grant_credits
    - POST is idempotent: skips grant_credits if stripe_session_id already processed
    - POST processes invoice.paid and calls grant_subscription_credits

    **__tests__/lib/actions/reviews.test.ts** (REV-01):
    - submitReview inserts review with rating and comment
    - submitReview returns error if rating is missing or out of range (1–5)
    - submitReview returns error if booking is not in completed status

    **__tests__/lib/actions/earnings.test.ts** (EARN-03):
    - requestPayout inserts row into payout_requests
    - requestPayout returns error if no pending balance
    - requestPayout returns error if not authenticated as teacher
  </behavior>
  <implementation>
    RED PHASE ONLY. Import from not-yet-created modules. Use mocks object pattern (see admin.test.ts). All tests should be written with it.toBeTruthy() / expect().toBe() patterns that will fail until GREEN implementations exist.

    For payments and webhook tests: mock the `stripe` module. The stripe package is not yet installed — use jest.mock('stripe', ...) so the test file can be parsed.

    For webhook test: mock `next/headers` with `{ headers: jest.fn() }` pattern.

    After writing all 7 files, run `npm test -- --passWithNoTests` to confirm the suite fails with "Cannot find module" errors (not syntax errors). If any file has a syntax error, fix it — only "Cannot find module" is the correct RED failure mode.
  </implementation>
</feature>

<verification>
After completion:
- `npm test -- --testPathPattern="utils/slots" --passWithNoTests` — fails with Cannot find module (RED)
- `npm test -- --testPathPattern="actions/availability" --passWithNoTests` — fails with Cannot find module (RED)
- `npm test -- --testPathPattern="actions/bookings" --passWithNoTests` — fails with Cannot find module (RED)
- All 7 test files exist on disk
</verification>

<success_criteria>
Seven RED test stub files exist. They fail with "Cannot find module" (not syntax errors). Future plans will make them go GREEN by implementing the production code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction/03-02-SUMMARY.md`
</output>
