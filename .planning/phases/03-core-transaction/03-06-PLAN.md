---
phase: 03-core-transaction
plan: "06"
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - app/[locale]/teacher/bookings/page.tsx
  - components/teacher/booking-action-forms.tsx
  - messages/en.ts
  - messages/de.ts
autonomous: true
requirements: [BOOK-03, BOOK-04, BOOK-05, BOOK-07, BOOK-08]

must_haves:
  truths:
    - "Teacher bookings page lists bookings grouped or sortable by status (pending, confirmed, completed, cancelled)"
    - "Pending bookings show Confirm and Decline buttons"
    - "Clicking Confirm expands an inline form asking for meeting link before submitting"
    - "Clicking Decline expands an inline form showing available alternative slots for the next 7 days (fetched from getAvailableSlotsAction); slot selection is optional — teacher can decline without offering one"
    - "Confirmed bookings show Mark complete and Cancel buttons"
    - "Mark complete triggers complete_booking RPC via markComplete action"
    - "ICS export link appears on confirmed bookings"
  artifacts:
    - path: "app/[locale]/teacher/bookings/page.tsx"
      provides: "Teacher booking queue UI"
      min_lines: 80
    - path: "components/teacher/booking-action-forms.tsx"
      provides: "Client components for confirm/decline/mark-complete inline forms"
  key_links:
    - from: "components/teacher/booking-action-forms.tsx"
      to: "lib/actions/bookings.ts"
      via: "confirmBooking, declineBooking, markComplete, cancelBooking"
      pattern: "confirmBooking|declineBooking|markComplete"
    - from: "components/teacher/booking-action-forms.tsx (DeclineBookingForm)"
      to: "lib/actions/availability.ts"
      via: "getAvailableSlotsAction(teacherId, dateStr)"
      pattern: "getAvailableSlotsAction"
---

<objective>
Build the teacher bookings page: the queue where teachers review pending requests, confirm with a meeting link, decline (with optional alternative slot offer), mark sessions complete, and export to calendar.

Purpose: BOOK-03 through BOOK-07 are teacher-side flows. This page is the teacher's primary operational view for the transaction loop.
Output: Fully functional teacher bookings page replacing the current EmptyState stub.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-transaction/03-CONTEXT.md
@.planning/phases/03-core-transaction/03-05-SUMMARY.md

Read node_modules/next/dist/docs/ for relevant Next.js guidance before writing code.

**Locked decisions:**
- Teacher sees pending requests on /teacher/bookings
- Confirm: inline form/field for Zoom/Meet link appears BEFORE submitting (not a separate page)
- Decline: teacher can optionally offer an alternative slot picked from their OWN current availability for the next 7 days (not free text — locked decision per CONTEXT.md). Slot selection is optional; teacher can still decline without offering one. The chosen slot (if any) is stored on the booking as proposed_alternative_start_time.
- Mark complete: teacher clicks after session ends; triggers complete_booking RPC
- ICS export: link/button on confirmed/upcoming bookings only

**getAvailableSlotsAction** is defined in lib/actions/availability.ts (added by plan 03-07 Task 1). However, because 03-06 and 03-07 run in the same wave, the executor must add this Server Action to lib/actions/availability.ts as part of THIS plan if 03-07 has not yet run. Check whether the function exists before adding it; if it is already present, import it directly.

The function signature to add (if missing):
```typescript
export async function getAvailableSlotsAction(
  teacherId: string,
  dateStr: string // YYYY-MM-DD
): Promise<string[]> {
  "use server";
  const date = new Date(dateStr + "T00:00:00");
  return getAvailableSlotsForDay(teacherId, date);
}
```

**proposed_alternative_start_time column:** Add this column to the bookings table as part of this plan's setup step. Run:
```sql
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS proposed_alternative_start_time TIMESTAMPTZ;
```
via a new migration file `supabase/migrations/20260601000002_bookings_alternative_slot.sql`, then regenerate types.

**Component patterns:**
- DashboardLayout with getTeacherNav
- Card/CardHeader for each booking (tone based on status)
- Badge/StatusBadge for booking status
- useActionState for form error display in Client Components
- Server Component page + Client Component action forms (split pattern used throughout Phase 2)
- requireRoleFromParams for auth guard

**Supabase join type handling:** getTeacherBookings returns students/profiles as nested — use casting pattern from Phase 2 (see 02-04-SUMMARY.md or 02-06-SUMMARY.md for precedent).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking action Client Components (including DeclineBookingForm with slot picker)</name>
  <files>components/teacher/booking-action-forms.tsx, lib/actions/availability.ts (extend if needed), supabase/migrations/20260601000002_bookings_alternative_slot.sql, types/database.ts</files>
  <action>
**Step 0 — Add proposed_alternative_start_time column:**
Create supabase/migrations/20260601000002_bookings_alternative_slot.sql:
```sql
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS proposed_alternative_start_time TIMESTAMPTZ;
```
Apply: `npx supabase db push` (or the project's migration command from package.json scripts).
Regenerate types: run the project's type-gen command (check package.json scripts; typically `npm run gen:types`).

**Step 1 — Ensure getAvailableSlotsAction exists:**
Open lib/actions/availability.ts. If `getAvailableSlotsAction` is not yet exported, append it to the file:
```typescript
export async function getAvailableSlotsAction(
  teacherId: string,
  dateStr: string // YYYY-MM-DD
): Promise<string[]> {
  "use server";
  const date = new Date(dateStr + "T00:00:00");
  return getAvailableSlotsForDay(teacherId, date);
}
```
This function is a thin wrapper around getAvailableSlotsForDay from lib/queries/availability.ts. Import getAvailableSlotsForDay if not already imported.

**Step 2 — Create components/teacher/booking-action-forms.tsx with `"use client"`:**

**ConfirmBookingForm({ bookingId }):**
- useState for expanded (boolean) — starts false
- When not expanded: renders a "Confirm" Button that sets expanded=true
- When expanded: renders a small form with:
  - Text input for meeting link (Zoom/Meet URL)
  - "Confirm session" submit button
  - "Cancel" button to collapse back (sets expanded=false)
  - useActionState(confirmBooking, {}) — show error if state.error exists
  - Hidden input for bookingId

**DeclineBookingForm({ bookingId, teacherId }):**
Implements the locked CONTEXT.md decision: teacher can optionally offer an alternative slot picked from their own current availability (not free text).

State:
- `expanded: boolean` — starts false
- `slots: string[]` — available ISO slot times; populated on expand
- `loadingSlots: boolean`
- `selectedSlot: string | null` — the chosen alternative slot (optional)
- `useActionState(declineBookingWithAlternative, {})` where declineBookingWithAlternative is defined below

When not expanded: renders a "Decline" Button that:
1. Sets expanded=true
2. Fetches available slots for the next 7 days: iterate dates from tomorrow to +7 days, call `getAvailableSlotsAction(teacherId, dateStr)` for each, flatten into one array (or fetch per day as user clicks — simpler: fetch all 7 days on expand and flatten). Load into `slots`.
3. Sets loadingSlots=false after all fetches complete

When expanded:
- If loadingSlots: show a small loading indicator ("Loading your availability...")
- If slots is empty (after loading): show "No available slots in the next 7 days. Decline without offering an alternative?" with a "Decline anyway" button
- If slots is non-empty: show a `<select>` dropdown:
  - First option: `<option value="">No alternative offered</option>` (default, unselected = plain decline)
  - Then one `<option>` per slot formatted as local date+time (e.g. "Mon 2 Jun, 14:00")
  - Use `new Date(slotIso).toLocaleString('de-CH', { weekday: 'short', day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' })` for display
- "Decline" submit button (always present whether slot is selected or not)
- "Cancel" button to collapse (sets expanded=false, resets selectedSlot)
- Show state.error if present

Form fields:
- Hidden `<input name="bookingId" value={bookingId} />`
- `<input name="proposedAlternativeStartTime" value={selectedSlot ?? ""} />` — empty string if no slot chosen

The Server Action called by this form is `declineBooking` from lib/actions/bookings.ts, extended to handle the optional proposedAlternativeStartTime. Update the `declineBooking` action in lib/actions/bookings.ts:
- After the existing cancel_booking RPC call succeeds, check if `formData.get("proposedAlternativeStartTime")` is non-empty
- If non-empty: `UPDATE bookings SET proposed_alternative_start_time = proposedAlt WHERE id = bookingId` (the booking is already cancelled, but we record the alternative for the student to see)
- revalidatePath remains unchanged

**MarkCompleteForm({ bookingId }):**
- Simple form with "Mark complete" Button
- useActionState(markComplete, {})
- Show loading state on submit

**CancelBookingForm({ bookingId }):**
- "Cancel booking" button with confirmation (expand pattern)
- useActionState(cancelBooking, {})

**IcsDownloadLink({ bookingId }):**
- Simple anchor tag: `<a href={`/api/bookings/${bookingId}/ics`} download>Export to calendar</a>`
- Styled as a text link or small button

All forms use hidden input for bookingId, import actions from lib/actions/bookings.ts.
Import getAvailableSlotsAction from lib/actions/availability.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>components/teacher/booking-action-forms.tsx exists with all 5 exported components. DeclineBookingForm fetches available slots via getAvailableSlotsAction and renders an optional slot picker dropdown. declineBooking action stores proposed_alternative_start_time when provided. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Teacher bookings page</name>
  <files>app/[locale]/teacher/bookings/page.tsx, messages/en.ts, messages/de.ts</files>
  <action>
Replace the stub app/[locale]/teacher/bookings/page.tsx:

Server Component structure:
1. Auth: requireRoleFromParams(params, "teacher") or existing pattern
2. Fetch: getTeacherBookings(profile.id) from lib/queries/bookings.ts
3. Group bookings by status: pending, confirmed (upcoming), completed, cancelled

Layout (inside DashboardLayout with getTeacherNav):
- If no bookings at all: render existing EmptyState with relevant message
- "Pending requests" section: list of booking cards for status='pending'
  - Card shows: student name, date/time, topic note
  - Actions: ConfirmBookingForm + DeclineBookingForm side by side
  - Pass teacherId to DeclineBookingForm so it can fetch the teacher's own availability
- "Upcoming sessions" section: status='confirmed'
  - Card shows: student name, date/time, meeting link (display as clickable URL)
  - Actions: MarkCompleteForm + CancelBookingForm + IcsDownloadLink
- "Completed" section (collapsed or at bottom): status='completed'
  - Card shows: student name, date/time, earnings recorded (if available)
- "Cancelled" section (collapsed or at bottom): status='cancelled'
  - If a cancelled booking has proposed_alternative_start_time set, display it as "Alternative offered: {formatted date/time}"

Use Card component with appropriate tone:
- pending → muted or inset
- confirmed → brand or soft
- completed → muted
- cancelled → muted

Format dates as locale-aware strings using `new Date(start_time).toLocaleDateString('de-CH', ...)` for Swiss date format.

Add i18n keys to messages/en.ts and messages/de.ts under teacher.bookings namespace ONLY (do not modify keys added by plan 03-03 under teacher.availability):
- pendingRequests, upcomingSessions, completedSessions, cancelledSessions
- noBookings, confirmSession, declineSession, markComplete, cancelBooking, exportCalendar
- topicNote, meetingLink, student
- alternativeOffered: "Alternative offered"
- noAlternativeSlots: "No available slots in the next 7 days"
- declineWithoutAlternative: "Decline without offering an alternative"
- loadingSlots: "Loading your availability..."
- offerAlternativeSlot: "Offer an alternative slot (optional)"

Run `npx tsc --noEmit` after completion.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Teacher bookings page renders with grouped sections. Confirm inline form, Decline with optional slot picker, Mark complete, Cancel, and ICS export all wired. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no errors
- Teacher bookings page at /[locale]/teacher/bookings renders (no crash with empty bookings)
- ConfirmBookingForm expands with meeting link input before submitting
- DeclineBookingForm expands and shows available slots from getAvailableSlotsAction; dropdown present; slot selection optional
- proposed_alternative_start_time column exists in bookings table after migration
</verification>

<success_criteria>
BOOK-03: Teacher sees pending booking requests.
BOOK-04: Teacher can confirm (with meeting link) or decline (with optional alternative slot from own availability — locked decision per CONTEXT.md).
BOOK-05: Meeting link provided on confirmation.
BOOK-07: Mark complete button triggers atomic RPC.
BOOK-08: Cancel button available on confirmed bookings.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction/03-06-SUMMARY.md`
</output>
