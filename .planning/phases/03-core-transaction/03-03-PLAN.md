---
phase: 03-core-transaction
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - lib/utils/slots.ts
  - lib/queries/availability.ts
  - lib/actions/availability.ts
  - app/[locale]/teacher/availability/page.tsx
  - messages/en.ts
  - messages/de.ts
autonomous: true
requirements: [AVAIL-01, AVAIL-02, AVAIL-03]

must_haves:
  truths:
    - "generateSlots utility returns correct 15-min increment slots for a day, excluding blocked dates and overlapping bookings"
    - "Teacher can add a weekly availability range (day + start/end time) from their availability page"
    - "Teacher can remove a weekly availability range from their availability page"
    - "Teacher can add a blocker date from their availability page"
    - "Availability ranges and blockers persist to the database"
  artifacts:
    - path: "lib/utils/slots.ts"
      provides: "Pure slot generation algorithm"
      exports: ["generateSlots"]
    - path: "lib/queries/availability.ts"
      provides: "DB queries for availability data"
      exports: ["getTeacherAvailabilityRanges", "getTeacherAvailabilityBlockers", "getAvailableSlotsForDay"]
    - path: "lib/actions/availability.ts"
      provides: "Server Actions for availability mutations"
      exports: ["setAvailabilityRange", "removeAvailabilityRange", "setBlocker", "removeBlocker"]
    - path: "app/[locale]/teacher/availability/page.tsx"
      provides: "Teacher availability management UI"
      min_lines: 80
  key_links:
    - from: "app/[locale]/teacher/availability/page.tsx"
      to: "lib/actions/availability.ts"
      via: "Server Action form submissions"
      pattern: "setAvailabilityRange|removeAvailabilityRange"
    - from: "app/[locale]/student/teachers/[id]/page.tsx (Plan 03-07)"
      to: "lib/queries/availability.ts"
      via: "getAvailableSlotsForDay"
      pattern: "getAvailableSlotsForDay"
---

<objective>
Implement the availability management system: slot generation utility, DB queries, Server Actions, and teacher availability page.

Purpose: Teachers need to declare their weekly schedule before any student can see or book slots. This plan makes AVAIL-01/02/03 functional at the library and teacher UI layer.
Output: Working teacher availability page where teachers can add/remove weekly ranges and blockers.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-transaction/03-CONTEXT.md
@.planning/phases/03-core-transaction/03-RESEARCH.md
@.planning/phases/03-core-transaction/03-01-SUMMARY.md

Read node_modules/next/dist/docs/ for relevant Next.js guidance before writing code.

**Locked decisions:**
- Day ranges only (day_of_week + start_time + end_time), not individual slots
- Blockers are date-specific (DATE only, not date+time)
- Slot increments are 15 minutes
- Lesson duration is 50 minutes (from config/pricing.ts lessonDurationMinutes)
- Slot overlap filter must include BOTH 'pending' AND 'confirmed' bookings — not just 'confirmed'
- All times stored as Europe/Zurich local time for v1 (Swiss-only platform)

**Existing patterns to follow:**
- requireRoleFromParams for auth guard (or requireRole if action is not locale-aware)
- getDictionary + localizedPath for i18n
- DashboardLayout with getTeacherNav for the page layout
- Server components + Server Actions pattern
- revalidatePath after every mutation
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot generation utility + availability queries</name>
  <files>lib/utils/slots.ts, lib/queries/availability.ts</files>
  <behavior>
    - generateSlots(params) returns [] when targetDate is in blockedDates array
    - generateSlots returns ISO string start times at 15-min increments within each range
    - generateSlots last slot start time + 50 min must not exceed range end time
    - generateSlots filters out slots overlapping with any booking in bookedSlots (status pending OR confirmed)
    - generateSlots overlap check: candidate overlaps if candidate.start < booked.end AND candidate.end > booked.start
    - getAvailableSlotsForDay fetches ranges, blockers, and bookings then calls generateSlots
    - getTeacherAvailabilityRanges returns all ranges for a teacher ordered by day_of_week, start_time
    - getTeacherAvailabilityBlockers returns all blockers for a teacher ordered by blocked_date
  </behavior>
  <action>
Create lib/utils/slots.ts:
- Import `lessonDurationMinutes` from `@/config/pricing`
- Export `generateSlots({ ranges, blockedDates, bookedSlots, targetDate, slotIncrementMinutes = 15 })`
- Implement exactly as specified in RESEARCH.md Pattern 2 with the overlap check using `status IN ('pending', 'confirmed')` note (overlap logic is in slots.ts; the query in availability.ts handles the status filter)
- All time arithmetic uses native Date — no date-fns dependency
- Times treated as Europe/Zurich local time: when constructing Date from 'HH:MM' strings, use targetDate's year/month/day in local time

Create lib/queries/availability.ts:
- `getTeacherAvailabilityRanges(teacherId)`: select from teacher_availability_ranges ordered by day_of_week ASC, start_time ASC
- `getTeacherAvailabilityBlockers(teacherId)`: select from teacher_availability_blockers ordered by blocked_date ASC
- `getAvailableSlotsForDay(teacherId, targetDate)`: parallel fetch ranges (by day_of_week = targetDate.getDay()), blockers (all for teacher), bookings (status IN ['pending','confirmed'] for that day); call generateSlots; return string[]

Run tests after implementation: `npm test -- --testPathPattern="utils/slots" --passWithNoTests`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="utils/slots" --passWithNoTests</automated>
  </verify>
  <done>slots.test.ts passes GREEN. lib/utils/slots.ts and lib/queries/availability.ts exist and are correctly typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Availability Server Actions + teacher availability page</name>
  <files>lib/actions/availability.ts, app/[locale]/teacher/availability/page.tsx, messages/en.ts, messages/de.ts</files>
  <behavior>
    - setAvailabilityRange returns error if not authenticated as teacher
    - setAvailabilityRange upserts a row to teacher_availability_ranges
    - removeAvailabilityRange deletes a range by id (only teacher's own ranges)
    - setBlocker upserts a row to teacher_availability_blockers
    - removeBlocker deletes a blocker by id
    - All actions call revalidatePath after successful mutation
  </behavior>
  <action>
Create lib/actions/availability.ts with `"use server"`:
- `setAvailabilityRange(_prev, formData)`: validate dayOfWeek (0-6), startTime, endTime (HH:MM format); requireRole("teacher"); get teacher record by profile_id; upsert to teacher_availability_ranges; revalidatePath("/", "layout"); return {}
- `removeAvailabilityRange(_prev, formData)`: validate id present; requireRole("teacher"); delete from teacher_availability_ranges where id = rangeId AND teacher_id = teacher.id; revalidatePath; return {}
- `setBlocker(_prev, formData)`: validate blockedDate (YYYY-MM-DD); requireRole("teacher"); upsert to teacher_availability_blockers; revalidatePath; return {}
- `removeBlocker(_prev, formData)`: delete from teacher_availability_blockers by id; revalidatePath; return {}

Replace stub app/[locale]/teacher/availability/page.tsx:
- Server Component; requireRoleFromParams for auth
- Fetch availability ranges and blockers via getTeacherAvailabilityRanges and getTeacherAvailabilityBlockers
- UI sections:
  1. "Weekly availability" — table/list of existing ranges (day name, start–end) with inline delete form using removeAvailabilityRange action; add new range form with day select (0-6 mapped to day names), start time input, end time input
  2. "Blocked dates" — list of blocker dates with delete form; add new blocker form with date input
- Use DashboardLayout with getTeacherNav, getDictionary
- Use existing Card, Button, Badge, Table components where appropriate
- Add i18n keys to messages/en.ts and messages/de.ts under teacher.availability namespace (title, weeklyAvailability, blockedDates, addRange, addBlocker, noRanges, noBlockers)

Run tests: `npm test -- --testPathPattern="actions/availability" --passWithNoTests`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="actions/availability" --passWithNoTests</automated>
  </verify>
  <done>availability.test.ts passes GREEN. Teacher availability page renders with add/remove controls. Server Actions persist to DB correctly.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="utils/slots|actions/availability"` — GREEN
- `npx tsc --noEmit` — no new errors
- Teacher availability page at /[locale]/teacher/availability renders without error (no crash on empty state)
</verification>

<success_criteria>
AVAIL-01/02 implemented: teacher can manage weekly ranges and blockers via the availability page.
AVAIL-03 foundation: getAvailableSlotsForDay query available for student-facing views (03-07).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction/03-03-SUMMARY.md`
</output>
