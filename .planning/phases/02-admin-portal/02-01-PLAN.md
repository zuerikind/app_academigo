---
phase: 02-admin-portal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - types/database.ts
  - supabase/migrations/20260530000001_admin_tables.sql
  - __tests__/lib/actions/admin.test.ts
  - __tests__/lib/queries/admin.test.ts
  - __tests__/components/ui/table.test.tsx
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-04
  - ADMIN-07
  - ADMIN-08
  - ADMIN-02
  - ADMIN-05
  - ADMIN-06

must_haves:
  truths:
    - "types/database.ts contains level_promotion_requests and payout_requests table definitions that TypeScript accepts without error"
    - "RED test stubs exist for all admin actions and queries — tests run and fail for the right reason (module not found or function does not exist)"
    - "Table component test stub exists and fails"
  artifacts:
    - path: "types/database.ts"
      provides: "level_promotion_requests and payout_requests table Row/Insert/Update types"
      contains: "level_promotion_requests"
    - path: "supabase/migrations/20260530000001_admin_tables.sql"
      provides: "SQL DDL for both missing tables"
      contains: "CREATE TABLE level_promotion_requests"
    - path: "__tests__/lib/actions/admin.test.ts"
      provides: "RED stubs for approveTeacher, approvePromotion, rejectPromotion, markPayoutProcessed"
      exports: []
    - path: "__tests__/lib/queries/admin.test.ts"
      provides: "RED stubs for getAdminTeachers, getAdminStudents, getAdminBookings, getAdminPayouts, getAdminStats"
      exports: []
    - path: "__tests__/components/ui/table.test.tsx"
      provides: "RED stub for Table component render"
      exports: []
  key_links:
    - from: "types/database.ts"
      to: "lib/queries/admin.ts"
      via: "TypeScript import — Database['public']['Tables']['level_promotion_requests']['Row']"
      pattern: "level_promotion_requests"
    - from: "types/database.ts"
      to: "lib/queries/admin.ts"
      via: "TypeScript import — Database['public']['Tables']['payout_requests']['Row']"
      pattern: "payout_requests"
---

<objective>
Create the two missing DB table definitions, write the SQL migration, and scaffold all three RED test files. No implementation yet — just the type contracts and failing stubs that later plans will make green.

Purpose: Later plans cannot safely query `level_promotion_requests` or `payout_requests` without TypeScript types. Test stubs provide the Nyquist verification harness for the entire phase.
Output: Updated `types/database.ts`, a SQL migration file, and three failing test stubs.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-portal/02-CONTEXT.md
@.planning/phases/02-admin-portal/02-RESEARCH.md
@.planning/phases/02-admin-portal/02-VALIDATION.md

<interfaces>
<!-- Existing Database type shape — executor must extend, not replace -->
<!-- From types/database.ts -->
```typescript
export type Database = {
  public: {
    Tables: {
      // ... profiles, students, teachers, subjects, teacher_subjects,
      // student_credits, bookings, credit_packages ...
    };
    Views: Record<string, never>;
    Functions: { student_available_credits: { Args: { p_student_id: string }; Returns: number; }; };
    Enums: Record<string, never>;
  };
};
```

<!-- Existing test mock pattern — from __tests__/lib/actions/auth.test.ts -->
```typescript
const mocks = {
  update: jest.fn(),
  revalidatePath: jest.fn(),
  requireRole: jest.fn(),
};
jest.mock("next/cache", () => ({ revalidatePath: (...args: unknown[]) => mocks.revalidatePath(...args) }));
jest.mock("@/lib/auth/session", () => ({ requireRole: (...args: unknown[]) => mocks.requireRole(...args) }));
jest.mock("@/lib/supabase/server", () => ({
  createClient: jest.fn().mockResolvedValue({
    from: jest.fn().mockReturnValue({
      update: (...args: unknown[]) => mocks.update(...args),
      eq: jest.fn().mockReturnThis(),
      select: jest.fn().mockReturnThis(),
      order: jest.fn().mockReturnThis(),
    }),
  }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add missing table types to database.ts + write SQL migration</name>
  <files>types/database.ts, supabase/migrations/20260530000001_admin_tables.sql</files>
  <behavior>
    - After edit, `tsc --noEmit` passes with no errors referencing level_promotion_requests or payout_requests
    - The two new Tables entries are present in the Database type's Tables object
  </behavior>
  <action>
**types/database.ts** — append two new table entries inside `Tables: { ... }` after `credit_packages`. Do NOT replace existing tables:

```typescript
level_promotion_requests: {
  Row: {
    id: string;
    teacher_id: string;
    requested_level: 'academigo_teacher' | 'verified';
    status: 'pending' | 'approved' | 'rejected';
    note: string | null;
    created_at: string;
    updated_at: string;
  };
  Insert: {
    id?: string;
    teacher_id: string;
    requested_level: 'academigo_teacher' | 'verified';
    status?: 'pending' | 'approved' | 'rejected';
    note?: string | null;
    created_at?: string;
    updated_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["level_promotion_requests"]["Insert"]>;
};
payout_requests: {
  Row: {
    id: string;
    teacher_id: string;
    amount_chf: number;
    status: 'pending' | 'processed';
    note: string | null;
    created_at: string;
    updated_at: string;
  };
  Insert: {
    id?: string;
    teacher_id: string;
    amount_chf: number;
    status?: 'pending' | 'processed';
    note?: string | null;
    created_at?: string;
    updated_at?: string;
  };
  Update: Partial<Database["public"]["Tables"]["payout_requests"]["Insert"]>;
};
```

**supabase/migrations/20260530000001_admin_tables.sql** — create both tables:

```sql
-- Migration: Admin portal tables
-- level_promotion_requests: teacher tier promotion requests for admin review
-- payout_requests: teacher payout requests for admin processing

BEGIN;

CREATE TABLE IF NOT EXISTS level_promotion_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  teacher_id UUID NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  requested_level TEXT NOT NULL CHECK (requested_level IN ('academigo_teacher', 'verified')),
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
  note TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS payout_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  teacher_id UUID NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  amount_chf NUMERIC(10,2) NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'processed')),
  note TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Indexes for admin list queries (ordered by created_at DESC)
CREATE INDEX IF NOT EXISTS level_promotion_requests_created_at_idx ON level_promotion_requests(created_at DESC);
CREATE INDEX IF NOT EXISTS payout_requests_status_idx ON payout_requests(status);

COMMIT;
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "level_promotion\|payout_requests" || echo "TYPE CHECK CLEAN"</automated>
  </verify>
  <done>types/database.ts compiles without errors; both table definitions are present; SQL migration file exists with CREATE TABLE statements for both tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold RED test stubs for admin actions, queries, and Table component</name>
  <files>__tests__/lib/actions/admin.test.ts, __tests__/lib/queries/admin.test.ts, __tests__/components/ui/table.test.tsx</files>
  <behavior>
    - `npx jest __tests__/lib/actions/admin.test.ts --passWithNoTests` exits 0 (module not found is a failing test, not a crash — use --passWithNoTests to allow)
    - When `lib/actions/admin.ts` does not exist, tests fail with "Cannot find module" — this is the expected RED state
    - Same for queries and table component
  </behavior>
  <action>
Create three test files using the mocks object pattern (NOT top-level const mocks) to avoid Jest hoisting TDZ issues — same as `__tests__/lib/actions/auth.test.ts`.

**__tests__/lib/actions/admin.test.ts:**
```typescript
/**
 * Unit tests for lib/actions/admin.ts
 * RED phase: lib/actions/admin.ts does not exist yet.
 * Tests will fail with "Cannot find module" until Plan 02-02 implements the actions.
 */

const mocks = {
  update: jest.fn(),
  revalidatePath: jest.fn(),
  requireRole: jest.fn(),
};

jest.mock("next/cache", () => ({ revalidatePath: (...args: unknown[]) => mocks.revalidatePath(...args) }));
jest.mock("@/lib/auth/session", () => ({ requireRole: (...args: unknown[]) => mocks.requireRole(...args) }));
jest.mock("@/lib/supabase/server", () => ({
  createClient: jest.fn().mockResolvedValue({
    from: jest.fn().mockReturnValue({
      update: (...args: unknown[]) => mocks.update(...args),
      eq: jest.fn().mockReturnThis(),
      select: jest.fn().mockReturnThis(),
      order: jest.fn().mockReturnThis(),
    }),
  }),
}));

import {
  approveTeacher,
  approvePromotion,
  rejectPromotion,
  markPayoutProcessed,
} from "@/lib/actions/admin";

function makeFormData(entries: Record<string, string>): FormData {
  const fd = new FormData();
  Object.entries(entries).forEach(([k, v]) => fd.set(k, v));
  return fd;
}

describe("approveTeacher", () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mocks.update.mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) });
    mocks.requireRole.mockResolvedValue(undefined);
  });

  it("returns error if teacherId is missing", async () => {
    const result = await approveTeacher({}, makeFormData({}));
    expect(result.error).toBeTruthy();
  });

  it("calls requireRole('admin') for defense-in-depth", async () => {
    await approveTeacher({}, makeFormData({ teacherId: "uuid-123" }));
    expect(mocks.requireRole).toHaveBeenCalledWith("admin");
  });

  it("calls revalidatePath after successful approval", async () => {
    await approveTeacher({}, makeFormData({ teacherId: "uuid-123" }));
    expect(mocks.revalidatePath).toHaveBeenCalled();
  });

  it("returns {} on success", async () => {
    const result = await approveTeacher({}, makeFormData({ teacherId: "uuid-123" }));
    expect(result).toEqual({});
  });
});

describe("approvePromotion", () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mocks.update.mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) });
    mocks.requireRole.mockResolvedValue(undefined);
  });

  it("returns error if requestId is missing", async () => {
    const result = await approvePromotion({}, makeFormData({}));
    expect(result.error).toBeTruthy();
  });

  it("returns {} on success", async () => {
    const result = await approvePromotion({}, makeFormData({ requestId: "uuid-456" }));
    expect(result).toEqual({});
  });
});

describe("rejectPromotion", () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mocks.update.mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) });
    mocks.requireRole.mockResolvedValue(undefined);
  });

  it("returns error if requestId is missing", async () => {
    const result = await rejectPromotion({}, makeFormData({}));
    expect(result.error).toBeTruthy();
  });

  it("returns {} on success", async () => {
    const result = await rejectPromotion({}, makeFormData({ requestId: "uuid-789" }));
    expect(result).toEqual({});
  });
});

describe("markPayoutProcessed", () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mocks.update.mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) });
    mocks.requireRole.mockResolvedValue(undefined);
  });

  it("returns error if payoutId is missing", async () => {
    const result = await markPayoutProcessed({}, makeFormData({}));
    expect(result.error).toBeTruthy();
  });

  it("returns {} on success", async () => {
    const result = await markPayoutProcessed({}, makeFormData({ payoutId: "uuid-abc" }));
    expect(result).toEqual({});
  });
});
```

**__tests__/lib/queries/admin.test.ts:**
```typescript
/**
 * Unit tests for lib/queries/admin.ts
 * RED phase: lib/queries/admin.ts does not exist yet.
 */

const mocks = {
  select: jest.fn(),
  eq: jest.fn(),
  order: jest.fn(),
  head: jest.fn(),
};

jest.mock("@/lib/supabase/server", () => ({
  createClient: jest.fn().mockResolvedValue({
    from: jest.fn().mockReturnValue({
      select: (...args: unknown[]) => { mocks.select(...args); return { eq: mocks.eq, order: mocks.order, data: [], error: null }; },
      eq: mocks.eq,
      order: mocks.order,
    }),
  }),
}));

import {
  getAdminTeachers,
  getAdminStudents,
  getAdminBookings,
  getAdminPayouts,
  getAdminStats,
} from "@/lib/queries/admin";

describe("getAdminTeachers", () => {
  it("returns an array", async () => {
    const result = await getAdminTeachers();
    expect(Array.isArray(result)).toBe(true);
  });
});

describe("getAdminStudents", () => {
  it("returns an array", async () => {
    const result = await getAdminStudents();
    expect(Array.isArray(result)).toBe(true);
  });
});

describe("getAdminBookings", () => {
  it("returns an array with no status filter", async () => {
    const result = await getAdminBookings();
    expect(Array.isArray(result)).toBe(true);
  });

  it("returns an array with a status filter", async () => {
    const result = await getAdminBookings("pending");
    expect(Array.isArray(result)).toBe(true);
  });
});

describe("getAdminPayouts", () => {
  it("returns an array", async () => {
    const result = await getAdminPayouts();
    expect(Array.isArray(result)).toBe(true);
  });
});

describe("getAdminStats", () => {
  it("returns an object with teacherCount, studentCount, bookingCount, pendingTeachers, pendingPromotions, pendingPayouts", async () => {
    const result = await getAdminStats();
    expect(result).toHaveProperty("teacherCount");
    expect(result).toHaveProperty("studentCount");
    expect(result).toHaveProperty("bookingCount");
    expect(result).toHaveProperty("pendingTeachers");
    expect(result).toHaveProperty("pendingPromotions");
    expect(result).toHaveProperty("pendingPayouts");
  });
});
```

**__tests__/components/ui/table.test.tsx:**
```typescript
/**
 * Unit tests for components/ui/table.tsx
 * RED phase: components/ui/table.tsx does not exist yet.
 */

import React from "react";
import { render, screen } from "@testing-library/react";
import { Table } from "@/components/ui/table";

type Row = { id: string; name: string };

const columns = [
  { key: "name", header: "Name", render: (row: Row) => row.name },
];

describe("Table", () => {
  it("renders column headers", () => {
    render(<Table columns={columns} rows={[{ id: "1", name: "Alice" }]} />);
    expect(screen.getByText("Name")).toBeInTheDocument();
  });

  it("renders row data", () => {
    render(<Table columns={columns} rows={[{ id: "1", name: "Alice" }]} />);
    expect(screen.getByText("Alice")).toBeInTheDocument();
  });

  it("renders emptyState when rows is empty", () => {
    render(
      <Table
        columns={columns}
        rows={[]}
        emptyState={<div>No data</div>}
      />
    );
    expect(screen.getByText("No data")).toBeInTheDocument();
  });

  it("renders null when rows is empty and no emptyState provided", () => {
    const { container } = render(<Table columns={columns} rows={[]} />);
    expect(container.firstChild).toBeNull();
  });
});
```
  </action>
  <verify>
    <automated>npx jest __tests__/lib/actions/admin.test.ts __tests__/lib/queries/admin.test.ts __tests__/components/ui/table.test.tsx --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Three test files exist on disk. Running jest with --passWithNoTests exits 0. Tests are in RED state (failing with "Cannot find module") — this is expected and correct for the Wave 0 stub phase.</done>
</task>

</tasks>

<verification>
After completing both tasks:
- `npx tsc --noEmit` shows no errors related to level_promotion_requests or payout_requests
- `npx jest --passWithNoTests` exits 0
- `types/database.ts` contains entries for `level_promotion_requests` and `payout_requests` inside Tables
- `supabase/migrations/20260530000001_admin_tables.sql` exists with both CREATE TABLE statements
- Three test files exist at the specified paths
</verification>

<success_criteria>
1. Both missing tables are defined in TypeScript types
2. SQL migration creates both tables in correct form
3. Three RED test stubs exist and run (failing for the correct reason — module not found, not syntax errors)
4. `npx jest --passWithNoTests` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-portal/02-01-SUMMARY.md`
</output>
