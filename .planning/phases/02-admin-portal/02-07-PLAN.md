---
phase: 02-admin-portal
plan: "07"
type: execute
wave: 5
depends_on:
  - "02-05"
  - "02-06"
files_modified: []
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08

must_haves:
  truths:
    - "All 8 ADMIN requirements are verified by a human admin user in a live browser session"
    - "Admin signs in and is redirected to /admin/dashboard — not a 404"
    - "Dashboard stat cards show numeric values for teachers, students, bookings; revenue shows stub text"
    - "Needs Attention section shows pending counts with working links"
    - "Teachers page renders table; Approve button on a pending teacher updates the row"
    - "Students page renders table with credit balances"
    - "Bookings page: tab filter changes the visible rows server-side"
    - "Promotions page: Review expands a row, Approve/Reject submits correctly"
    - "Payouts page: Mark Processed button updates payout status"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 2 admin portal deliverables. All automated tests pass before reaching this checkpoint — this plan confirms the live browser experience works correctly end to end.

Purpose: Automated tests verify function contracts. This checkpoint verifies the rendered UI, navigation flow, and real Supabase interactions that tests cannot cover.
Output: Confirmed admin portal is fully functional. Phase 2 complete.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite before handing to human</name>
  <files></files>
  <action>
Run the full Jest test suite to confirm all tests pass before the human verification step.

```bash
npx jest --passWithNoTests
```

If any tests fail, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. Output shows 0 failed test suites.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser verification of all admin portal pages</name>
  <what-built>
Complete admin portal:
- ADMIN-01: /admin/dashboard page (stat cards + needs-attention inbox)
- ADMIN-02: /admin/teachers (teacher list with approval status and tier)
- ADMIN-03: Inline Approve button on pending teacher rows
- ADMIN-04: /admin/promotions (expandable row with Approve/Reject + note)
- ADMIN-05: /admin/students (student list with credit balance)
- ADMIN-06: /admin/bookings (status tab filter via URL params)
- ADMIN-07: /admin/payouts (pending payout requests)
- ADMIN-08: Mark Processed button on payout rows
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`

2. Sign in with an admin account at http://localhost:3000/de/login
   - Confirm you are redirected to /de/admin/dashboard (not 404, not login loop)

3. Dashboard (ADMIN-01):
   - Stat cards show numeric values for Total Teachers, Total Students, Total Bookings
   - Revenue card shows "— (Phase 3)" stub text — not an error
   - Needs Attention section shows pending counts with working links

4. Teachers page (ADMIN-02 + ADMIN-03):
   - Navigate to /de/admin/teachers
   - Table renders with columns: Name, Email, Level, Status, Joined, Actions
   - Pending teachers show yellow "Ausstehend" badge and "Freigeben" button
   - Click Approve on a pending teacher — page refreshes, teacher status changes to green "Freigegeben"

5. Students page (ADMIN-05):
   - Navigate to /de/admin/students
   - Table renders with Name, Email, Credits (available/total), Joined columns

6. Bookings page (ADMIN-06):
   - Navigate to /de/admin/bookings
   - Tab row shows: Alle | Ausstehend | Bestätigt | Abgeschlossen | Storniert
   - Click "Ausstehend" tab — URL changes to ?status=pending, table shows only pending bookings
   - Click "Alle" tab — URL loses ?status param, all bookings show

7. Promotions page (ADMIN-04):
   - Navigate to /de/admin/promotions
   - Table renders with pending promotion requests (or empty state if none)
   - If pending requests exist: click "Prüfen" — row expands with note textarea + Freigeben + Ablehnen buttons
   - Approve or reject one — row collapses and status updates

8. Payouts page (ADMIN-07 + ADMIN-08):
   - Navigate to /de/admin/payouts
   - Table renders with pending payouts (or empty state if none)
   - If pending payouts exist: click "Als verarbeitet markieren" — status updates to "Verarbeitet"

9. Navigation (all pages):
   - Admin sidebar shows: Dashboard, Lehrpersonen, Lernende, Buchungen, Stufenaufstiege, Auszahlungen
   - All 6 nav links navigate to their respective pages without 404
  </how-to-verify>
  <resume-signal>
Type "approved" if all pages render and actions work correctly.
Describe any issues found (page, behavior, error message) if something is broken.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms all admin pages render and all actions (approve teacher, approve/reject promotion, mark payout processed, bookings tab filter) work in the live browser.
</verification>

<success_criteria>
1. All Jest tests pass (Task 1 automated gate)
2. Human confirms each admin page renders without error
3. All 8 ADMIN requirements verified in live browser
4. Phase 2 admin portal is complete and ready for Phase 3 dependency (at least one approved teacher exists)
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-portal/02-07-SUMMARY.md`

Update ROADMAP.md Phase 2 status to "Complete" and mark all 8 ADMIN-XX requirements as [x] in REQUIREMENTS.md.
Update STATE.md: status → "executing", current_phase → 3.
</output>
